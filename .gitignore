# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# LSD build output written during manual runs / eval
tests/cases/*/actual/

# Secrets / local env — NEVER commit real API keys.
# `env/` above only ignores the virtualenv dir, not a `.env` file, so list it here.
.env
.env.*
!.env.example
*.pem
*.key
secrets.*
*.local

# Editor / OS
.DS_Store
*.swp
